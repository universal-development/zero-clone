# Empty rclone.conf is fine for local-to-local syncs.
# In real usage, add your remotes here:
# [server-a]
# type = sftp
# host = server-a.example.com
#
# [server-b]
# type = s3
# provider = AWS
# region = us-east-1
