# Global init for zero-clone.
# This file is sourced automatically when zero-clone is run from this directory.
# Use it to set project-wide defaults before CLI arguments are processed.

# Override the default clone directory name ("clone") for all bases in this project.
# Can also be set on the command line with: zero-clone --clone-dir NAME
# export ZERO_CLONE_DIR=data
