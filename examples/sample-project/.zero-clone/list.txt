## Example list of rclone sync sources. Format: SRC [DEST]
## Lines starting with # are comments; blank lines are ignored.

## Example jobs (local paths or remotes). For tests we rewrite this file.
# /absolute/path/to/src/alpha a
# /absolute/path/to/src/beta
